#include<iostream>
using namespace std;
int p[105],sm[105];
int pack=0;
inrt mi=99999999;
int dp(int m,int n)
{
    if(m==1)
        return sm[pack-1]-sm[n-1];
    else
        {
            mi=(max())
        }
}
int main()
{
    int m,ans=0;
    cin>>m>>pack;
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
        ans+=a[i];
        sm[i]=ans;
    }
    if(m==1)
        cout<<s[n-1]<<endl;
        cout<<dp(m,pack)<<endl;
}
